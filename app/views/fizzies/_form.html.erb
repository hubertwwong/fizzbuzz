<%= form_for(@fizzy) do |f| %>
  <% if @fizzy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fizzy.errors.count, "error") %> prohibited this fizzy from being saved:</h2>

      <ul>
      <% @fizzy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
